import time
from selenium import webdriver

video_link = 'https://youtu.be/YQfGH-dfM7k'
views = 1000
video_duration = 30 * 60

driver = webdriver.Chrome()
driver.get(video_link)

for i in range(views):
    time.sleep(video_duration)
    driver.refresh()
